import { FileImage } from 'lucide-react';

export default function FilesImages() {
	return (
		<div className='relative flex items-center justify-center'>
			<FileImage size={48} strokeWidth={1.5} />
		</div>
	);
}
